# 語言規則

無論使用者用什麼語言提問，一律使用**繁體中文**回覆與溝通。

程式碼中的所有註解一律使用**繁體中文**撰寫，不使用英文註解。變數名稱、函式名稱等識別字維持英文（沿用專案現有慣例），只有註解內容需要中文。

此規則沒有例外，即使使用者用英文提問，也一律以中文回覆。

這條規則也涵蓋回覆過程中的所有文字輸出，包括工具呼叫前後的簡短狀態說明、進度更新、思考過程摘要等——不是只有「最終答案」才要用中文，任何會顯示給使用者看的文字都必須是繁體中文，不能因為只是「順口的過程敘述」就切回英文。

---

# 內容創作硬性規則（違反即重做）

這個專案是兩章式虛構 ARG（另類實境遊戲）：第一章《靖河市協尋案》（`prequel/`）、第二章《靖河市第四案》（repo 根目錄）。以下規則對**兩章共同適用**，任何新增或修改的劇情內容都不得違反：

1. **全部內容為虛構。** 所有人名、地名、機構、案件編號、帳號、電話、日期皆為虛構作品設定。
2. **永遠不得輸出任何形似真實地址的字串。** 沒有門牌、沒有巷弄號碼、沒有座標、沒有可貼進地圖的東西。
3. **不得連向任何真實網站、真實二手平台、真實協尋機構。** 所有「外部頁面」都是本專案自己做的假頁面。
4. **不得要求玩家去真實世界做任何事**（不查證、不搜尋、不聯絡任何人）。全部互動封閉在站內。
5. 首次進站顯示一次性內容警示：本作為虛構作品，含失蹤、死亡與暴力暗示。可跳過，不可省略。
6. 玩家自填的暱稱與文字只存在 `localStorage`，不上傳、不外送。
7. **全站任何頁面都不放真人照片（含案件照片、頭像）。** 案件/貼文照片一律用文字說明或抽象色塊/emoji 佔位帶過，避免虛構案件被誤認成真實協尋案件、也避免圖庫真人被誤認成故事角色。
8. **第二章「他」全劇沒有台詞、沒有帳號、沒有暱稱，一次都不出現。** 任何謎題的答案都不得是他的名字或身分。
9. **第二章「周妤」（前作角色）本作只出現一行**（`legacy.html` 的求助句）。不得移植前作的主動提示聊天機制。◆ 已知的刻意例外：徵信錄謎題會透過一封不具名的匿名信主動點出「這裡怪怪的」（`trust.html`→`worksheet.html` 流程）。這是使用者明確要求、知情推翻「不主動遞提示」原則的決定，不是疏忽——底線仍守住：這封信不是周妤寫的、也不是他寫的，全程不具名、不建立新角色。
10. **第二章永遠不解釋的清單**（下列九件事）不得被任何 UI、謎題、彩蛋解答：
    1. 那三個同班同學裡，誰做了那張圖
    2. 07:14 那張照片的右下角
    3. 陳語安怎麼知道那條近路
    4. `014.txt` 是誰
    5. `007.txt` 為什麼在 2024/03/11 被更新
    6. 林昭與林晞的死因、死亡時間、中間那幾年
    7. `015.txt` 為什麼是 06:52
    8. 門後面有沒有人
    9. `016.txt` 是誰的

---

# 已知技術地雷／踩坑筆記

這節記錄程式碼裡「為什麼要這樣做」「已知限制」「權宜之計」「曾經修過的 bug 模式」——修改對應機制前請先讀這裡，避免重蹈覆轍。檔案位置以 `claude/prequel-front-bridge` 合併當下（commit `233b735`）為準，之後若有搬移請自行核對。

## 時間軸／日期一致性

- 第一章鎖定畫面／主畫面（`prequel/assets/js/shell.js`）與第二章主畫面（`scripts/shell.js`）的日期、天氣都是**寫死的固定值**，不抓玩家裝置的即時系統時間——分別對應「網站架好隔天」「林晞遺體公告當天」這兩個故事時間點。改殼層時間顯示邏輯前，先想清楚會不會戳破案件時間軸矛盾。
- 第一章結局頁 `prequel/ending/index.html` 的 `013.txt` 建檔日期也是刻意寫死落在第一章時間線內、且首次顯示與之後重訪會刻意差一天（暗示「這份檔案在你填表單之前就已經存在」），不是 bug。
- 第一章 `prequel/files/collection.html` 的 `007.txt`「最後更新 2024/03/11」早於「建檔 2022/02/19」看起來矛盾，是刻意的敘事設計（暗示檔案持續被更新過），不要「修正」它。

## 全螢幕請求：多點接力，不能只在一處呼叫

換頁必然重置全螢幕狀態（瀏覽器安全限制，無法跨頁保留），所以請求全螢幕的呼叫點故意分散在流程多處接力，而非集中一次：`prequel/entry.html`（玩家第一次點擊）、鎖定畫面解鎖、殼層「瀏覽器」App 圖示、`armFullscreenOnFirstClick()`（`browser-chrome.js` / `scripts/chrome.js`，掛在每個內容頁）。

- `armFullscreenOnFirstClick()` 的旗標檢查刻意放在**點擊當下**而非掛載時判斷，因為玩家可能在旗標被設定「之前」就已經掛載了某頁；也刻意不用 `{ once: true }`，因為玩家可能連續按 Esc 跳出全螢幕多次，每次點擊都該重試。
- `resources/help.html` 這類刻意不掛假瀏覽器外框（要跳出敘事）的頁面，仍然要手動補上全螢幕重試監聽器，否則玩家在此頁按 Esc 後無法自動接回全螢幕——**移除某個共用元件時，記得檢查它身上是否掛了其他頁面隱性依賴的機制**（這頁同時也要手動補 `window.jhGoBackInSite`，否則殼層返回鍵在此頁會找不到函式）。
- `walk.html`／`admin.html`／`gone.html`（`NO_CHROME_FILES`）不掛假網址列，但仍要呼叫 `armFullscreenOnFirstClick()`——尤其 `walk.html` 是整段流程最後、最需要維持全螢幕的地方。

## 站內導覽紀錄取代原生瀏覽器歷史

假瀏覽器的上一頁／下一頁與 Android 返回鍵不能用原生 `history.back()/forward()`——如果玩家在 iframe 最一開始那頁還沒往下點過任何連結，原生「上一頁」其實是進站前瀏覽器停留的真實網頁，按下去會真的離開整個網站。改成自己在 `sessionStorage` 記一份站內專用瀏覽紀錄（`jh_nav_stack`），**兩章共用同一份鍵名**（反正現在是同一個網站），走到底就不做事。

## 章節守衛（`chapter-guard.js`）的非同步陷阱

`location.href` 觸發的導頁不是立即生效，同一份文件裡剩下的 `<script>` 標籤仍會照樣執行完。任何跟在章節守衛後面、且會「記錄玩家逛過此頁」的邏輯（例如 `browser-chrome.js` 的 `trackAppTabVisit()`），都必須**重新檢查一次同樣的 `data-requires` 條件**，否則玩家明明被彈開、根本沒看到內容，該頁仍會被誤判成「已經逛過」，導致多工畫面出現不該出現的卡片。這是實際修過的 bug（對應 commit `694f2bb`），加新的「進頁即記錄」邏輯時務必比照處理。

## 面板疊層與 z-index 收合順序

- Android 三鍵返回鍵依 z-index 由上而下逐層收合（多工畫面 → 訊息／備忘錄面板 → 委派給 iframe 內 `jhGoBackInSite()`），不會直接穿透去換頁，避免玩家分不清楚返回鍵到底有沒有反應。
- 切到別的畫面前要先呼叫 `closeAllPanelsForSwitch()` 把其他蓋在上面的面板收乾淨——例如備忘錄還開著時跳出新訊息通知，若沒收乾淨，點通知只會在備忘錄底下默默打開訊息面板（備忘錄 z-index 較高，畫面上完全看不出變化，玩家會以為點了沒反應）。Home 鍵情境用另一支 `closeAllPanelsForHome()`，因為 Home 鍵是徹底離開、不需要「記得等一下退回去」。
- `hideHomeScreen()` 除了淡出外層，還必須額外移除 `.jh-boot__home--visible`，因為主畫面本身有 `pointer-events:auto` 規則讓 App 圖示點得到，光淡出外層不夠，透明的主畫面仍會擋住底下 iframe 的點擊——這是曾經真實出現過的 bug 模式。
- `messagesOpenedFromBrowser`／`notesOpenedFromBrowser` 這類「開面板前是否已在主畫面」的旗標，是為了決定關閉面板後該留在主畫面還是跳回原本在讀的內容，改動面板開關邏輯時不要漏掉這個分支。

## 訊息推送系統

- 周妤傳訊息前的打字動畫（跳動的「…」泡泡＋隨機停留 1~3 秒）用**佇列**而非各自獨立的 `setTimeout` 串接，避免多句話同時進來時動畫疊在一起或顯示順序錯亂。
- 用 `navigator.webdriver === true` 偵測自動化測試環境，是的話延遲直接歸零——這是為了不讓既有的 E2E 測試斷言（大量 `waitForTimeout` 依賴訊息時序）因為多了隨機延遲而變得不穩定。新增任何「真人感」的隨機延遲效果，都要記得加這個判斷。
- 密碼類訊息刻意設計成「純文字，不能整則點得進去」，連結另外用一則「本身就是網址」的獨立訊息傳——不然玩家會分不清楚這則訊息是「告訴你密碼」還是「點這裡進去」（對應 commit `1cb00d0`，這條規則貫穿全部訊息推送邏輯，新增訊息類型時要遵守）。
- `CLUE_REQUESTS`（玩家主動選擇提交、可能選錯，需要線索選擇器）與 `QUICK_REPLIES`（內容頁已經是明確發現，只需按送出）是刻意區分的兩套機制，對應「玩家主動確認」與「內容頁被動觸發」兩種敘事節奏，新增劇情推進點時要先想清楚屬於哪一種，不要混用。
- 線索確認題刻意設計成「中性系統提示」（灰字置中，不套訊息泡泡樣式），不能讓周妤看起來像「主動跳出來問」——她沒有理由已經知道玩家剛剛在哪頁發現了什麼。唯一允許她主動開口的情境是她自己架的協尋站（她看得到玩家在自己網站上的行為，例如 `rec1029.html` 滑到頁尾這一步）。
- 線索系統的「確認」動作只掛載在殼層訊息串裡，**不要**在內容頁上另外寫一份 UI；內容頁只負責 `JHNotebook.add(...)` 記線索 + `JHPhone.pushClue(...)` 請周妤發問。

## 加好友機制的敘事順序保護

周妤刻意不是一開始就在通訊錄裡，玩家必須先主動加好友（`xunren/correction.html`）她才會出現在聯絡人清單、計入未讀角標。在那之前任何一頁已記下的線索確認提示仍會照常寫進訊息紀錄，只是不計未讀、不出現在清單——這是刻意設計，不是資料遺失，除錯時看到「訊息不見了」先檢查是不是還沒加好友。

## 多工畫面「動態 App」

社交媒體／二手平台的多工畫面卡片只在玩家實際逛過對應板塊之後才出現（`app_social_unlocked`／`app_market_unlocked`），對應真實手機「多工只列出實際開過的 App」。玩家若目前停留在這兩個板塊，點擊「網站」多工卡片或主畫面「瀏覽器」圖示要先強制導回 `home.html` 再進入，因為那兩個板塊各自有專屬分頁卡片、各記各的「上次逛到哪裡」。

## i18n 三語系統

第一章（`prequel/assets/js/i18n.js`）與第二章（`scripts/i18n.js`）是**兩份完全獨立、機制不同**的實作，沒有共用程式碼：

- 第一章走「遍歷 DOM 文字節點＋屬性（`placeholder`/`aria-label`/`title`/`alt`），原文存進 `WeakMap`，用 `MutationObserver` 監看動態插入內容」的運行時全頁翻譯，不需要每頁手動呼叫翻譯函式。查字典時**先試完全原文，查不到才退回摺疊空白後的版本**（多數文字節點的縮排只是排版方便，但少數保留原始換行的內容——如結局逐字稿、ASCII 版本樹——不能被摺疊處理影響）。
- 「固定文字＋動態內容拼在同一個文字節點」的情況（例如「9 則・賣家回覆 5 則」）字典無法窮舉組合，要退回 `JH_I18N_EN_PATTERNS` regex 分離動態片段、遞迴翻譯固定片段。
- 玩家自由輸入文字（暱稱、how_known）混在固定標籤裡的頁面（如 `ending/index.html` 的 `013.txt`），標籤要另外用語言物件手動挑選，不能依賴 i18n 自動翻譯機制去處理任意輸入。
- `document.title` 不在 `document.body` 裡，DOM 遍歷碰不到，需要獨立腳本（`ambient.js`）手動判斷語言切換。
- 簡體字對照表（`t2s-map.js`）是用 opencc-js（tw2cn 標準）逐字核對過全站實際出現的繁體字產生的，不是憑印象手動列的——新增文字內容後如果簡體顯示不對，先檢查是不是漏了對照表項目，而不是懷疑轉換邏輯。
- 第一章／第二章的語言選擇分別存在各自前綴的 localStorage（`jh1f_lang` vs `jh4:locale`），互不影響，玩家在兩章需要分別切換語言，這是預期行為不是 bug。

## 恐怖層（Ambient Horror）刻意不持久化

以下效果刻意**不寫進 localStorage**，重新整理不保證還在——這是設計，不要「修好」成永久生效：
- `social/lin-xi.html` 重訪限時動態，50% 機率多出凌晨 03:14 全黑的第 7 則
- `social/lin-zhao-2019.html` 確認身分後重訪，按讚數 50% 機率偏移 ±1~2
- `social/rec1029.html` 玩家暱稱 50% 機率出現在一則舊留言裡
- `chat/thread.html` 跑完終幕後重訪，`m10` 時間戳 50% 機率偏移 ±1 分鐘
- `files/collection.html` 假問卷填完（是/否皆同）沒有任何反應，只是不重複彈出

`social-likes.js` 按讚數直接讀「畫面上目前顯示的數字」做加減，不另存基準值——即使恐怖層腳本已經偷改過顯示數字，按讚/收回的加減邏輯仍要以畫面現況為準。

## 其他資料完整性細節

- 累計「實際在畫面前」時間（`state.js` 的 `activeMs()`）只算 `document.visibilityState === visible` 的區間，單次累加上限 2 分鐘，避免電腦睡眠等異常情況灌入離譜數字。
- 謎題答案比對前先 `normalize()`：去除頭尾空白、統一全形冒號/井號（`：`→`:`、`＃`→`#`），避免玩家輸入全形符號被誤判答錯。
- `chat/thread.html` 的訊息重排謎題用寫死的固定亂序 `[3,5,0,4,1,2]`，不是真隨機——保證每次都有解謎挑戰性，不會恰好洗牌成正確順序。
- `market/data.json`（147 筆黑膠交易列表）的留言資料刻意生成「賣家回覆數 < 買家詢問數」，強化「這個帳號長期已讀不回」的敘事真實感——之後若要重新生成填充資料，記得保留這個約束。

## Admin／Debug 面板

- 兩章的除錯功能已整併到根目錄 `admin.html`（`prequel/debug/index.html` 現在只是轉址頁），不要在 `prequel/` 底下新增獨立的 debug 頁面。
- `admin.html` 的密碼門（`ADMIN_PW`）只是防止玩家不小心滑到管理面板，不是真的安全機制，不要在這個假設上做任何安全性相關判斷。
- admin 解鎖狀態存在獨立 key（`jhAdminUnlocked`，無章節前綴），兩章各自的「清空進度」按鈕都不會洗掉它——新增「清空進度」類按鈕時，不要不小心把這個 key 也清掉。
