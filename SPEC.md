# 靖河市第四案 — 實作規格（SPEC.md）

給 AI coding agent 讀。敘事來源：`靖河市第四案_劇本.md`。文本資料：`content.json`。
視覺設計另有 `DESIGN_BRIEF.md`，交給 Gemini。

---

## 0. 硬性規則（違反即重做）

1. **全部內容為虛構。** 所有人名、地名、機構、案件編號、帳號、電話、日期皆為虛構作品設定。
2. **永遠不得輸出任何形似真實地址的字串。** 沒有門牌、沒有巷弄號碼、沒有座標、沒有可貼進地圖的東西。第六幕只能用步行描述（見 §7）。
3. **不得連向任何真實網站、真實二手平台、真實協尋機構。** 所有「外部頁面」都是本專案自己做的假頁面。
4. **不得要求玩家去真實世界做任何事**（不查證、不搜尋、不聯絡任何人）。全部互動封閉在本站內。
5. 首次進站顯示一次性告示：本作為虛構作品，含失蹤、死亡與暴力暗示。可跳過，不可省略。
6. 玩家自填的暱稱與文字只存在 `localStorage`，不上傳、不外送。

---

## 1. 技術選型

| 項目 | 決定 | 理由 |
|---|---|---|
| 型態 | 純靜態站，無後端 | 部署在 GitHub Pages |
| 框架 | **無框架**。原生 HTML + CSS + ES module JS | 作者用手機開發，不要 build step |
| 路由 | **hash routing**（`#/case/JH-2026-004`） | GitHub Pages 沒有 SPA rewrite |
| 資料 | 單一 `content.json`，啟動時 fetch 一次 | 文本與程式分離，方便改稿 |
| 狀態 | `localStorage`，key 前綴 `jh4:` | 需跨分頁保存進度 |
| 音效 | `<audio>` 元素，僅在使用者點擊後播放 | 瀏覽器自動播放限制 |

> ⚠️ 若在 Claude artifact 內預覽，`localStorage` 不可用 —— 預覽時降級為記憶體內物件，正式部署再開啟。程式需以一層 `store.js` 封裝，切換只改一處。

---

## 2. 檔案結構

```
/index.html
/styles/            ← Gemini 負責，見 DESIGN_BRIEF.md
/scripts/
  main.js           路由與啟動
  store.js          進度儲存（localStorage / memory 兩種實作）
  router.js         hash routing
  render.js         頁面渲染
  flags.js          旗標與解鎖判定
  endgame.js        第六幕狀態機
/data/content.json
/audio/
  door_key.mp3      鑰匙轉動 3s
  door_hinge.mp3    門軸 1s
```

---

## 3. 進度旗標

`store` 內存一個扁平物件，全部布林或字串：

```js
{
  nickname: "",          // 序章填寫
  howYouKnew: "",        // 序章填寫
  seen: {},              // { "JH-2022-002": true, ... } 看過的頁面 id
  midUpdateFired: false, // 第四幕中段更新是否已觸發
  collectionUnlocked: false,
  refusedCount: 0,       // 第六幕點「否」的次數
  finished: false        // 看完紅畫面
}
```

**解鎖規則**

| 事件 | 條件 |
|---|---|
| 中段更新（林晞尋獲公告） | `seen` 內含 `JH-2026-004` 且 `seen` 內含任兩個舊案 |
| 收藏頁 `#/collection` | 三個 `.txt` 頁面都看過後，`.txt` 頁尾出現一個 `../` 連結 |
| 第六幕 `#/walk` | 收藏頁看完，且徵信錄、存檔頁、二手快照三頁都看過 |
| 白屏封站 | `finished === true` 之後，任何路由都導向 `#/gone` |

---

## 4. 路由表

| 路由 | 頁面 | 備註 |
|---|---|---|
| `#/` | 基金會首頁 | 中段更新後首頁多一則公告 |
| `#/intro` | 序章：暱稱 + 「你是怎麼知道這件事的」 | 只出現一次 |
| `#/case/:id` | 案件頁 | 五個案件，見 content.json |
| `#/trust` | 年度徵信錄（假 PDF 檢視頁） | 第六幕第一步 |
| `#/archive2019` | 基金會 2019 舊版存檔頁 | 第六幕第二步 |
| `#/market/rec_1029` | 二手平台賣場快照 | 第六幕第三、四步 |
| `#/legacy` | 周妤的舊協尋網站（已停止更新） | 前作連結 |
| `#/file/:id` | `.txt` 純文字頁 | 007 / 012 / 015 |
| `#/collection` | 收藏，列出 001–015 | 隱藏 |
| `#/walk` | 第六幕 | 見 §7 |
| `#/gone` | 封站白頁 | 見 §8 |

---

## 5. 案件頁規格

每個案件頁的欄位順序**固定且完全相同**，包含已結案的：

```
案件編號 / 姓名 / 失蹤時年齡 / 失蹤日期 / 最後目擊地點
狀態  [協尋中|已尋獲|已結案]
案情摘要
協尋啟事 PDF（假的，點了開圖片）
相關連結
[捐款支持我們]        ← 每頁同一位置，永遠在
```

**◆ 制式化本身就是恐怖來源。不要為林晞或陳語安做特殊排版。**

---

## 6. 中段更新（第四幕）

觸發後：

1. 首頁頂端插入一則公告卡（見 content.json `announcement`）。
2. `JH-2025-003` 狀態由 `協尋中` 改為 `已尋獲`，並補上遺體描述欄位。
3. `#/file/012` 底部多一行 `最後更新 2026/08/17`。
4. **不做任何提示、不跳通知、不加動畫。** 玩家自己回首頁時才會發現。

---

## 7. 第六幕狀態機（`endgame.js`）

`#/walk` 進入後為線性五段，每段點一次「繼續」推進。

```
step 0  徵信錄結論   → 一個行政區
step 1  存檔頁結論   → 一個里
step 2  面交點結論   → 幾條街
step 3  窗景結論     → 樓層與朝向
step 4  步行文字     → 三行，逐行淡入，每行間隔 1.2s
step 5  選擇
step 6  開門
step 7  紅
```

### step 4｜步行

只輸出這三行，逐行出現，不得增加任何細節：

```
巷子。機車。鐵門。
樓梯間的聲控燈，走三階才亮。
五樓。
```

### step 5｜選擇

```
你要殺了他嗎？
[ 是 ]  [ 否 ]
```

- `refusedCount === 0` → 兩顆按鈕正常。
- 點「否」→ `refusedCount = 1`，**「否」按鈕直接從 DOM 移除**（不是變灰、不是動畫），剩一顆「是」。
- 此時若玩家不點，等 6 秒 → 「是」旁邊重新長出一顆按鈕，文字為 `否`。點它 → `refusedCount = 2`，該按鈕文字原地變成 `你已經走到這裡了。`，且不可點擊。
- 3 秒後，該元素文字變成 `是`，恢復可點擊。此時畫面上兩顆按鈕都是「是」。
- 點任一顆 → step 6。

> ◆ 全程不得有任何說明文字、旁白、提示。玩家必須自己發現選項被拿走了。

### step 6｜開門

畫面全黑（`#000`，覆蓋全視窗，`z-index` 最高）。

```
t=0.0   播放 door_key.mp3      3.0s
t=3.0   播放 door_hinge.mp3    1.0s
t=4.0   ── 完全靜音 1.0s ──    ★ 不可省略，不可縮短
t=5.0   進入 step 7
```

音訊必須在 step 5 的點擊事件中先 `unlock`（建立 AudioContext / 呼叫一次靜音播放），否則行動裝置會擋。

`prefers-reduced-motion` 下仍照播，但淡入淡出改為直接切換。
另提供一顆常駐的靜音鈕（右下角，極小），關掉音效不影響時序。

### step 7｜紅

- 滿版純紅，`4.0s` 內畫面上沒有任何東西。
- `t=4.0` 右下角以極小字級淡入：

```
016.txt　建檔　{今天的日期，格式 YYYY/MM/DD}
```

- `t=5.0` 設 `finished = true`，導向 `#/gone`。

---

## 8. `#/gone`

白底，黑字，置中偏上，無任何樣式：

```
社團法人臺灣失蹤兒少協尋關懷基金會
尋人資料庫　目前無法使用
```

底下保留那顆**捐款支持我們**按鈕，可點，點了什麼都不會發生。

**◆ 這顆按鈕是全作最後一個笑話，不准拿掉。**

---

## 9. 收藏頁

純文字，等寬字，無樣式，像 directory listing：

```
001.txt
002.txt
...
007.txt   林　昭    建檔 2022/02/19   最後更新 2024/03/11
012.txt   林　晞    建檔 2023/08/14   最後更新 2026/08/17
013.txt   {store.nickname}
014.txt   —
015.txt   陳語安    建檔 2026/08/26 06:52
```

`013.txt` 顯示玩家在序章填的暱稱。若為空字串，顯示 `—`。
點 `013.txt` → 顯示玩家在序章填的「你是怎麼知道這件事的」原文，以及一行 `建檔 {玩家首次進站日期}`。
點 `014.txt` → 空白頁，只有一行 `—`。

---

## 10. 交付順序

1. Claude：`store.js` / `router.js` / `flags.js` 骨架 + `content.json` 完整資料 + 各頁面純結構 HTML（無樣式）。
2. Gemini：依 `DESIGN_BRIEF.md` 產出 `/styles/`，只碰 CSS，不動 DOM 結構與 class 名稱。
3. Claude：`endgame.js` 第六幕演出與音訊時序。
4. 全站檢查：鍵盤可操作、行動裝置寬度、`prefers-reduced-motion`、關閉音效仍可完成。

**class 命名由 Claude 先定，寫在 `DESIGN_BRIEF.md` 附錄，Gemini 只能沿用。**
